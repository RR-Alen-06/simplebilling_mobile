import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF4F46E5); // Indigo 600
  static const Color primaryLight = Color(0xFF818CF8);
  static const Color primaryDark = Color(0xFF3730A3);

  static const Color secondary = Color(0xFF0D9488); // Teal 600
  static const Color accent = Color(0xFFF59E0B); // Amber 500

  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;
  static const Color surfaceVariant = Color(0xFFF1F5F9);

  static const Color textPrimary = Color(0xFF0F172A); // Slate 900
  static const Color textSecondary = Color(0xFF64748B); // Slate 500
  static const Color textMuted = Color(0xFF94A3B8);

  static const Color border = Color(0xFFE2E8F0);
  static const Color divider = Color(0xFFF1F5F9);

  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // --- Bold Pastel & High-Contrast Tokens (Neo-Brutalist Block Styling) ---
  static const Color neoBorder = Color(0xFF1E293B); // Slate 800 bold outline

  // Mint (Sales, Revenue, Success)
  static const Color pastelMint = Color(0xFFD1FAE5);
  static const Color deepMint = Color(0xFF065F46);
  static const Color borderMint = Color(0xFFA7F3D0);

  // Coral / Peach (Expenses, Alerts, Discounts)
  static const Color pastelCoral = Color(0xFFFFE4E6);
  static const Color deepCoral = Color(0xFF9F1239);
  static const Color borderCoral = Color(0xFFFECDD3);

  // Sky Blue (Customers, POS, Sync)
  static const Color pastelSky = Color(0xFFE0F2FE);
  static const Color deepSky = Color(0xFF075985);
  static const Color borderSky = Color(0xFFBAE6FD);

  // Amber / Butter (Pending Dues, Xerox Presets, Warnings)
  static const Color pastelAmber = Color(0xFFFEF3C7);
  static const Color deepAmber = Color(0xFF92400E);
  static const Color borderAmber = Color(0xFFFDE68A);

  // Lavender / Violet (Profit, Catalog, Invoices)
  static const Color pastelLavender = Color(0xFFEDE9FE);
  static const Color deepLavender = Color(0xFF5B21B6);
  static const Color borderLavender = Color(0xFFDDD6FE);

  // Lime (Quick Add, Active Presets)
  static const Color pastelLime = Color(0xFFECFCCB);
  static const Color deepLime = Color(0xFF3F6212);
  static const Color borderLime = Color(0xFFD9F99D);

  // Rose / Pink (Special Services, Custom Xerox)
  static const Color pastelRose = Color(0xFFFCE7F3);
  static const Color deepRose = Color(0xFF9D174D);
  static const Color borderRose = Color(0xFFFBCFE8);

  // Pink aliases
  static const Color pastelPink = Color(0xFFFCE7F3);
  static const Color deepPink = Color(0xFF9D174D);
  static const Color borderPink = Color(0xFFFBCFE8);

  // Shadow Light for Neo-Brutalist elevation
  static const Color shadowLight = Color(0x1A000000);
}
